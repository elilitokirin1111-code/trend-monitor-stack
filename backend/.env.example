# HotPush 配置文件
# 复制此文件为 .env 并填写配置

# ============ 基础配置 ============
DEBUG=true

# ============ 用户认证配置 ============
# 设置管理员密码后才会启用认证（必须设置）
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your_secure_password

# ============ RSSHub 配置 ============
# 主实例地址（强烈建议自建实例以获得最佳稳定性）
# 自建教程: https://docs.rsshub.app/deploy/
RSSHUB_URL=https://rsshub.rssforever.com

# 备用实例（逗号分隔），主实例失败时自动切换
RSSHUB_FALLBACK_URLS=https://rsshub.feeded.xyz,https://hub.slarker.me

# 抓取间隔（分钟）
FETCH_INTERVAL_MINUTES=5
HOTSPOT_COLLECTION_INTERVAL_MINUTES=30

# 失败重试次数
FETCH_RETRY_COUNT=2

# ============ 推送渠道配置 ============
# 至少配置一个推送渠道

# Telegram（推荐）
# 1. 找 @BotFather 创建 Bot，获取 Token
# 2. 找 @userinfobot 获取你的 Chat ID
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Discord Webhook
# 服务器设置 -> 整合 -> Webhook -> 创建 Webhook
DISCORD_WEBHOOK_URL=

# 企业微信群机器人
# 群设置 -> 群机器人 -> 添加机器人
WECOM_WEBHOOK_URL=

# 飞书群机器人
# 群设置 -> 群机器人 -> 添加机器人
FEISHU_WEBHOOK_URL=

# 钉钉群机器人
# 群设置 -> 智能群助手 -> 添加机器人
DINGTALK_WEBHOOK_URL=

# 邮件推送
# EMAIL_SMTP_HOST=smtp.gmail.com
# EMAIL_SMTP_PORT=587
# EMAIL_USERNAME=
# EMAIL_PASSWORD=
# EMAIL_FROM=

# ============ AI 摘要配置（可选） ============
# 支持 OpenAI、Claude、DeepSeek、Ollama 等（通过 litellm）
# 模型名格式参考: https://docs.litellm.ai/docs/providers
# OpenAI: gpt-4o-mini, gpt-4o
# Claude: claude-sonnet-4-20250514, claude-haiku-4-20250414
# DeepSeek: deepseek/deepseek-chat
# Ollama (本地): ollama/llama3
AI_MODEL=gpt-4o-mini
AI_API_KEY=
# 自定义 API 地址（Ollama 等本地模型需要设置）
# AI_BASE_URL=http://localhost:11434

# ============ 数据库配置 ============
# MySQL（必须配置）
# 需要先创建数据库: CREATE DATABASE hotpush CHARACTER SET utf8mb4;
DATABASE_URL=mysql://user:password@localhost:3306/hotpush

# ============ Redis 配置 ============
# 用于热榜缓存和推送去重
REDIS_URL=redis://localhost:6379/0
