services:
  # MySQL 数据库
  mysql:
    image: mysql:8.0
    container_name: hotpush-mysql
    environment:
      # ⚠️ 生产环境请修改此密码！
      - MYSQL_ROOT_PASSWORD=hotpush123
      - MYSQL_DATABASE=hotpush
      - MYSQL_CHARACTER_SET_SERVER=utf8mb4
      - MYSQL_COLLATION_SERVER=utf8mb4_unicode_ci
    # 不对外暴露，只在内部网络访问
    expose:
      - "3306"
    volumes:
      - mysql_data:/var/lib/mysql
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - hotpush-network

  # Redis 缓存
  redis:
    image: redis:alpine
    container_name: hotpush-redis
    # 不对外暴露，只在内部网络访问
    expose:
      - "6379"
    volumes:
      - redis_data:/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - hotpush-network

  # 后端 API
  backend:
    build: ./backend
    container_name: hotpush-backend
    expose:
      - "8000"
    environment:
      - TZ=Asia/Shanghai
      - DEBUG=false
      # 数据库配置（密码需与上方 MySQL 配置一致）
      - DATABASE_URL=mysql://root:hotpush123@mysql:3306/hotpush
      # Redis 配置
      - REDIS_URL=redis://redis:6379/0
      # ⚠️ 管理员账号（生产环境必须修改密码！）
      - ADMIN_USERNAME=admin
      - ADMIN_PASSWORD=admin123  # 请修改为强密码
      # JWT 密钥（用于用户登录令牌加密，固定值可防止重启后登录失效）
      # ⚠️ 请替换为任意随机字符串，建议 16 位以上，例如：openssl rand -base64 32
      - JWT_SECRET=please-change-this-to-a-random-string
      # RSSHub 实例配置
      - RSSHUB_URL=http://rsshub:1200
      - HOTSPOT_RSSHUB_BASE_URL=http://rsshub:1200
      - HOTSPOT_DAILYHOTAPI_BASE_URL=http://dailyhotapi:6688
      # Windows 主机 OpenCLI 只读桥（小红书登录态；见 scripts/start-opencli-bridge.ps1）
      - HOTSPOT_OPENCLI_BRIDGE_URL=${HOTSPOT_OPENCLI_BRIDGE_URL:-}
      - HOTSPOT_OPENCLI_BRIDGE_TOKEN=${HOTSPOT_OPENCLI_BRIDGE_TOKEN:-}
      # WeKnora 知识库（可选；密钥只从本机 .env 注入，不写入代码）
      - WEKNORA_BASE_URL=${WEKNORA_BASE_URL:-}
      - WEKNORA_API_KEY=${WEKNORA_API_KEY:-}
      - WEKNORA_KNOWLEDGE_BASE_ID=${WEKNORA_KNOWLEDGE_BASE_ID:-}
      - WEKNORA_TIMEOUT_SECONDS=${WEKNORA_TIMEOUT_SECONDS:-30}
      - FETCH_INTERVAL_MINUTES=5
      - HOTSPOT_COLLECTION_INTERVAL_MINUTES=30
      # AI 摘要配置（可选，支持 OpenAI/Claude/DeepSeek/Ollama 等）
      # - AI_MODEL=gpt-4o-mini
      # - AI_API_KEY=your_api_key
      # - AI_BASE_URL=https://api.openai.com/v1
      # 推送渠道配置（取消注释并填写）
      # - TELEGRAM_BOT_TOKEN=your_bot_token
      # - TELEGRAM_CHAT_ID=your_chat_id
      # - DISCORD_WEBHOOK_URL=your_webhook_url
      # - WECOM_WEBHOOK_URL=your_webhook_url
      # - FEISHU_WEBHOOK_URL=your_webhook_url
      # - DINGTALK_WEBHOOK_URL=your_webhook_url
    volumes:
      - ./data:/app/data
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
      rsshub:
        condition: service_healthy
      dailyhotapi:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - hotpush-network

  # 前端
  frontend:
    build: ./frontend
    container_name: hotpush-frontend
    ports:
      - "3001:80"  
    depends_on:
      - backend
    restart: unless-stopped
    networks:
      - hotpush-network

  # RSSHub 数据源
  rsshub:
    image: diygod/rsshub:chromium-bundled
    container_name: hotpush-rsshub
    ports:
      - "1200:1200"
    environment:
      - NODE_ENV=production
      - CACHE_TYPE=memory
      - CACHE_EXPIRE=300
      # 微博 Cookie（可选，用于获取更多微博数据）
      # - WEIBO_COOKIE=your_weibo_cookie_here
      # B站 Cookie（可选，用于获取更多B站数据）
      # - BILIBILI_COOKIE=your_bilibili_cookie_here
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:1200 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    networks:
      - hotpush-network

  # DailyHotApi 数据源（固定镜像摘要，供抖音/B站及微博 fallback）
  dailyhotapi:
    image: imsyy/dailyhot-api@sha256:748fc79d29a422f512018e8a58ef1a0e20306dd6254637933960573fa37b1e5e
    container_name: hotpush-dailyhotapi
    expose:
      - "6688"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://localhost:6688').then(r=>{if(!r.ok)process.exit(1)}).catch(()=>process.exit(1))"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 20s
    networks:
      - hotpush-network

volumes:
  mysql_data:
  redis_data:

networks:
  hotpush-network:
    driver: bridge
